RecentActivity.ps1 parses entries from UserAssist and JumpLists to give recent activity by user

SecurityAge.ps1 is a super fast way to find out how far back a Security Event log reaches

ScheduledTasks.ps1 lists all Scheduled Tasks on a host, including Last Run and Last Modified.  Includes a -Days option to limit number of days to search.
